id: OBS-20260805-017
at: 2026-08-05T11:11:15Z
source: task_outcome
claim: "Saved-fixture replay cannot detect that a writer and its checker have drifted apart, because the frozen artifact and the frozen contract are the same vintage; only a live session runs the current writer against the current checker."
context:
  task: "R5.5 six-session gate — Host reruns"
  paths:
    - scripts/eval-kickoff.mjs
    - skills/kg-kickoff/scripts/record-turn.mjs
    - scripts/test-v2.mjs
evidence:
  - { type: log, ref: "D72: all three fresh kickoff sessions failed while Part 6's all_six_fixtures_pass_m5_evaluators stayed green — the six saved fixtures hold v1 turn products and eval-kickoff reads KICKOFF_TURN_SCHEMA.legacy_field_order and asserts version === 1, so replay compared an old artifact against an old contract" }
  - { type: log, ref: "R5.2 upgraded record-turn.mjs to emit v2 with turn_id, sequence and user_message_index whenever a session records more than one turn, which is the normal case for a grill skill; G-K1 escaped two of three failures only because that session happened to record a single turn" }
  - { type: log, ref: "Same round, second symptom: R5.2 moved deep retrieval into a script so the agent no longer reads sources directly (KN-0042), but forbid_fabrication still demanded a runner file_read event per cited source, and must_find reported three sources missing that were present in kickoff-context.json" }
urgency: batch
