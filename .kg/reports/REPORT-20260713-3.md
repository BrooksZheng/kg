# kg 编译报告 — 2026-07-13 第 3 轮(批处理,13 条观察,人工触发)

人工触发的批处理会话。inbox 13/13 校验通过,无残留 round log。

## 观察处理(13 条)

| 观察 | 裁定 | 动作 |
| --- | --- | --- |
| OBS-20260713-002(task_outcome) | project_contract | **update KN-0003**——registry 安装(`npx skills add` 只拷贝技能目录)并入既有 _lib.mjs/布局契约:claim 扩展覆盖三种安装模式,新增 vendored-copies-committed + sync-vendored --check 防漂移条款(已 log-update) |
| OBS-20260713-003(task_outcome) | project_knowledge | **新增 KN-0007**(active)——ESM main-module 判定必须 realpath argv[1],否则 symlink 安装的脚本静默跳过 CLI 入口 |
| OBS-20260713-005(task_outcome) | project_knowledge | **新增 KN-0008**(active)——Claude Code 只读 CLAUDE.md、只从 .claude/skills/ 发现技能;kg-init 条件化双桥接 |
| OBS-20260713-006(human_decision, R1) | project_contract | **新增 KN-0009**(candidate)+ Q-20260713-001——条目原子性契约,约束编译器本身的 merge 纪律 |
| OBS-20260713-007(human_decision, R2) | **no_change** | 裁决已完整记录于 docs/rfc-002 R2(confirmed);domains 必填将随 S1 落入 knowledge.schema.yaml(机器载体)。现在抄成条目 = 制造第二真相源(R8 明确拒绝的模式) |
| OBS-20260713-008(human_decision, R3) | **no_change** | 同上(rescope 动作,S1 落入 routing.yaml + kg-compile SKILL) |
| OBS-20260713-009(human_decision, R4) | **no_change** | 同上(分层渲染 a+,S1 落入 agents-block.mjs) |
| OBS-20260713-010(human_decision, R5) | **no_change** | 同上(指针行兜底,S1 落入渲染器) |
| OBS-20260713-011(human_decision, R6) | **no_change** | 同上(新域人审,S2 落入词表校验 + proposal 流) |
| OBS-20260713-012(human_decision, R7) | **no_change** | 同上(AGENTS.md 整体产物化,S3 落入组装器) |
| OBS-20260713-013(human_decision, R8) | **no_change** | 同上(三源分治,S3 落入 section 映射表) |
| OBS-20260713-014(human_decision, R9) | **no_change** | 同上(分级预算,S1 落入 config + 渲染器) |
| OBS-20260713-015(human_decision, R10) | project_contract | **新增 KN-0010**(candidate)+ Q-20260713-002——两层论宪法定位:补丁政策与报警条件约束当下的每一次工程判断,不等实施分片 |

**R1/R10 与 R2–R9 的区分标准**:R1(原子性)和 R10(两层论)从此刻起就约束编译器
行为与工程判断,须经索引可发现;R2–R9 是待实施的设计,其最终载体是机器面
(schema/routing/渲染器),RFC-002 已作为 confirmed 的 formal_decision 载体完整
记录裁决与被拒方案——现在复制成条目正是 R8 拒绝的「第二真相源」模式。
S1–S3 实施时,对应观察的裁决语义会随实现进入机器载体。

## 碰撞检查

- KN-0007(realpath main 判定)vs KN-0005(realpath src/dest 身份判定):同用
  `fs.realpathSync`,但 claim 不同(CLI 入口检测 vs 破坏性刷新守卫),非重复。
- RFC-002 R3(rescope 合法)vs 当前 routing.yaml `actions` 未含 rescope:非冲突——
  RFC-002 明文「本文只记录设计与裁决,协议/脚本改动按 §六分片另行实施」,
  实施前 rescope 不可用,属预期状态,无需 conflict 队列项。
- KN-0003 扩展 vs KN-0006:前者管布局不变量,后者管默认分支分发语义,互补印证。

## 减法答卷(必答)

本轮 0 merge / 0 demote / 0 retire,新增 4。原因:10 条条目逐对核对无真重复或
被替代关系(唯一疑似对 KN-0005/KN-0007 经核实为不同 claim);收敛发生在编译前——
OBS-002 并入 KN-0003 而非独立成条,8 条 no_change 拒绝复制已有载体的知识。
备注:待 S1 落地 rescope 后,「有 scope.paths 的 knowledge 条目下沉」将提供
新的减法出路;本轮 8 条 active 渲染 16/30 行,预算尚有余量。

### kg metrics (machine-computed)

- repeat-correction count (cumulative across all rounds, not new-this-round): 0
- candidate acceptance rate: 2/2 accepted — 2 still pending
- knowledge regret log: empty
- subtraction ratio this round: 0/4 (merge+demote+retire / add) — additions without subtraction; state explicitly in the report why nothing could be merged or retired

## 裁决清单(pending)

- **Q-20260713-001**(promotion)——激活 KN-0009(条目原子性/渲染层治理契约)。
  推荐:accept——RFC-002 R1 已由人明确裁决并 confirmed,条目只是把该裁决
  发布为编译期约束。
- **Q-20260713-002**(promotion)——激活 KN-0010(两层论:台账层是产品,渲染层
  是耗材;补丁政策 + 报警条件)。推荐:accept——人的原话「可以,写进 RFC-002」,
  条目使宪法定位从索引可发现。

另报备(非队列项):KN-0003(active 契约)本轮经 sanctioned hand-edit 扩展了
claim 措辞(并入 registry 安装模式),索引行随之变化;扩展方向与 Q-20260712-001
已接受的契约一致,如有异议可在裁决面谈中提出回退。

## 待修复观察

无(13/13 校验通过,全部处理并归档)。
