# kg 编译报告 — 2026-07-13 第 2 轮（fast-track，单条观察）

触发：OBS-20260713-004（`human_correction`，fast_track）——人工实测 `npx skills add`
安装后运行 kg-init 报 `cannot locate shared lib`。

## 观察处理

| 观察 | 判定 | 去向 |
| --- | --- | --- |
| OBS-20260713-004 | `project_knowledge`（auto 层） | 新增 KN-0006（active） |

KN-0006：`npx skills add owner/repo` 只分发**默认分支**；未合并的分支修复对消费者不可见，
且分支名含 `/` 的 tree URL 会被 CLI 在第一个 `/` 处截断（`%2F` 编码也无效）。
测试未合并分支需从本地 clone 安装。权威级 `verified_runtime_behavior`（实测复现）。

碰撞检查：与 KN-0001…KN-0005 无冲突——KN-0004 说"分发 UX 应收敛到 registry 式安装"，
KN-0006 补充其分支语义，互相印证而非矛盾。

范围外未动：OBS-20260713-002 / 003 / 005 仍留在 inbox（3/5，未到阈值）。

## 减法答卷（必答）

本轮 0 减法（0 merge / 0 demote / 0 retire，新增 1）。原因：KN-0006 是全新的分发语义
事实，不复述、不取代任何现有条目；现有 6 条 active 条目各自 scope 独立，无可合并项。

## 指标（机器计算，逐字嵌入）

- repeat-correction count (cumulative across all rounds, not new-this-round): 0
- candidate acceptance rate: 2/2 accepted
- knowledge regret log: empty
- subtraction ratio this round: 0/1 (merge+demote+retire / add) — additions without subtraction; state explicitly in the report why nothing could be merged or retired

## 裁决清单

`.kg/queue/` 无 pending 项（Q-001、Q-002 均已 accepted 并执行）。本轮无新队列项。
