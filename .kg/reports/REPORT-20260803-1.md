# kg compile report — 2026-08-03 round 1

Session: Host (evaluator role), post-M3-closure batch compile.
Input: 6 pending observations (threshold 5 exceeded), 1 accepted registered
document (`docs/rfc-004-authoring-plane-and-brownfield-bootstrap.md`).

## Routing

| Observation | Verdict | Target |
|---|---|---|
| OBS-20260731-019 | add | KN-0030 declared paths stored, canonical paths compared |
| OBS-20260731-020 | add | KN-0031 plans must own implicitly created ancestors |
| OBS-20260731-021 | **update** | KN-0024 gained clause 3: terminal state excludes per-run diagnostics |
| OBS-20260731-022 | add | KN-0032 four properties of an honest test seam |
| OBS-20260803-001 | add (candidate) | KN-0033 re-derive the boundary at the acting component |
| OBS-20260803-002 | **update** | KN-0023 gained the mutable-target TOCTOU double check |

Two of six observations were absorbed as updates to existing entries rather
than new entries. That is the dedupe pass doing its job: OBS-021 is a
refinement of KN-0024's idempotent-completion clause, and OBS-002 extends
KN-0023's snapshot binding from immutable sources to mutable targets.
Publishing them as new entries would have fragmented two claims into four.

## Ruling checklist

- Q-20260803-001 (promote KN-0033 to project_contract) — **accepted** under
  the standing human delegation of queue rulings recorded 2026-07-31. The
  queue is now fully resolved: 12/12.

## Subtraction

Subtraction ratio is 0/4 and the reason is not neglect. The only entry that
spec-revC §五 marks as superseded is KN-0013 (AGENTS.md managed-block line
budget). Its cited mechanism was deleted in v2 — there is no managed block,
no `agents_block_budget_lines`, and no `render-agents.mjs` — but its claim
(always-on surface stays a pointer index; subtract rather than enlarge)
survives v2 unchanged and in fact tightened, from a rendered budget to a
30-line hand-kept file. Retiring it would have dropped the reasoning while
keeping the rule, so it was rebound by update instead. Every other v1 entry
is marked 沿用 / 演变 / 部分沿用 in the same table; none is false today.

Recommendation carried forward: KN-0013's size rule is now prose with no
gate behind it, which by KN-0012 will rot silently. An AGENTS.md size check
belongs in kg-scan's M4 deterministic metrics.

## Round action log

Also included in this round's action log are the two promotions executed on
2026-07-31 under the same delegation (KN-0028, KN-0029 candidate → active).
They were carried in `.round-actions.jsonl` from the ruling session, which
ran after the previous report was written; they are accounted for here
rather than discarded.

## Notable finding

There is no writer script that owns queue resolution. Recording a ruling
means hand-editing `.kg/queue/*.yaml`, which immediately produced a
duplicate `resolution_note` key this round (the template already carries
`resolution_note: null`). That violates KN-0028 — machine surfaces should be
produced only by validated writers. Recorded as an observation for M4.

### kg metrics (machine-computed)

- repeat-correction count (cumulative across all rounds, not new-this-round): 0
- candidate acceptance rate: 12/12 accepted
- knowledge regret log: empty
- subtraction ratio this round: 0/4 (merge+demote+retire / add) — additions without subtraction; state explicitly in the report why nothing could be merged or retired
- staleness sweep (last_verified older than 60 days, live entries): none
