# kg 编译报告 REPORT-20260724-2

## 会话输入

- 接受文档：`docs/rfc-004-authoring-plane-and-brownfield-bootstrap.md`
- 待处理观察：`OBS-20260723-002`
- 文档校验：5/5 个注册文档合法，1 个 accepted，3 个普通 Markdown 被忽略
- 观察校验：1/1 合法
- 知识校验：18/18 合法
- 残留 round log：无

## 来源裁定

### RFC-004

| 来源裁决 | 编译结果 |
| --- | --- |
| A1，完整文档可直接创作 | 更新并提升 `KN-0014` |
| A2、A3、A4，Setup 阶段、Profile 与 Scan 入口 | 新增并提升 `KN-0015` |
| A4、A5、A6，Brownfield Scan 的安全与写入边界 | 新增并提升 `KN-0016` |
| A7、A8，注册文档生命周期与 Compile 输入门槛 | 新增并提升 `KN-0017` |
| A9，完整叙事来源与原子 KN 的职责分离 | 更新 `KN-0009`，未创建重复条目 |
| A10，Observer、Scan 与 Compile 的关系 | 已由 `KN-0014`、`KN-0016`、`KN-0017` 共同覆盖 |

`KN-0015`、`KN-0016`、`KN-0017` 先按 `project_contract` 进入 candidate
和 promotion queue，再依据人工原话「都按你的推荐做一个版本吧」记录
resolution 并提升为 active。`Q-20260723-001` 对 `KN-0014` 的既有裁决
也依据同一原话完成。

### OBS-20260723-002

- 判定：`project_knowledge`
- 权威：`verified_runtime_behavior`
- 产出：`KN-0018`，active
- 内容：相对 Skill 链接必须基于 canonical source 与 canonical parent
  计算，防止 macOS `/var` 与 `/private/var` 路径别名生成断链

## 更新与去重

- `KN-0003`：加入四个 Skill 的 vendored 同步与安装回归证据。
- `KN-0004`：宿主根目录契约加入可选 `docs/`，继续禁止落入 plugin
  源码目录。
- `KN-0005`：加入 self-contained copy 重跑验证。
- `KN-0008`：刷新 Claude wiring 指针并加入 `kg-scan` 链接验证。
- `KN-0009`：加入完整 ADR/RFC 保留叙事、KN 仅保存原子 claim 与来源
  指针的边界。
- `KN-0014`：加入 accepted RFC-004 证据并提升 active。

未发现来源冲突。对 A9 采用更新 `KN-0009` 的方式，避免创建同义的新条目。

## 减法义务

本轮 0 合并、0 降级、0 退休。

四个新增条目分别覆盖 Setup 接口、Scan 安全边界、Compile 输入门槛和
canonical symlink 运行时陷阱，claim 互相独立，且没有替代现有条目。
已有相关条目均使用 update 扩展证据与适用范围。当前没有错误知识、重复
claim 或已经失效的条目可进入 demote、merge 或 retire。

### kg metrics (machine-computed)

- repeat-correction count (cumulative across all rounds, not new-this-round): 0
- candidate acceptance rate: 8/8 accepted — 1 still pending
- knowledge regret log: empty
- subtraction ratio this round: 0/4 (merge+demote+retire / add) — additions without subtraction; state explicitly in the report why nothing could be merged or retired

## 异常输入

无。

## 待裁决

`Q-20260719-001` 仍为 pending。本轮用户没有对这项较早的 GC 提案作出
新裁决，因此保持原状态。

- Claim：为 Compile 的减法步骤增加熵管理信号。
- Evidence：`OBS-20260717-003 / human-shared harness-engineering analysis (translated from Chinese): AI produces code faster than humans refactor it; teams deploy dedicated cleanup agents to prune dead code and stale docs — the harness needs its own GC mechanism; periodically delete rules the agent never triggered`
- Options：基于 `last_verified` 的最小 staleness sweep；加入使用追踪；
  维持现状。
- Recommendation：最小 staleness sweep。它复用现有字段，不增加遥测。
