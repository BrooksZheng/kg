# kg fast-track compile report 2026-07-24

## 本轮输入与裁定

- `OBS-20260723-001`：有效的 `human_correction`。
- 路由：`project_contract`。
- 产出：`KN-0014`，当前为 `candidate`。
- 人审：`Q-20260723-001`，当前为 `pending`。
- 冲突检查：未发现与现有条目的实质冲突。`KN-0014` 澄清了
  `KN-0009` 的适用边界。原子性约束作用于 `knowledge/*.md` 台账条目，
  不限制 `docs/**` 下完整协作文档的直接起草。

## 新增与更新

- 新增 `KN-0014`：KG 必须允许人和 Agent 直接起草及迭代完整 ADR、
  RFC 与 MVP 技术方案文档。Observer 可以从中学习，Compile 可以在
  决策形成后提取原子知识，但文档创建本身不受管道前置约束。
- 本轮无现有条目更新。

## 减法义务

本轮没有合并、降级或淘汰。现有 13 条 active 条目各自承载独立 claim，
没有与新候选形成真重复或覆盖关系。新候选用于补齐此前缺失的文档协作边界，
保留为独立契约有利于后续验证和追责。

### kg metrics (machine-computed)

- repeat-correction count (cumulative across all rounds, not new-this-round): 0
- candidate acceptance rate: 4/4 accepted — 2 still pending
- knowledge regret log: empty
- subtraction ratio this round: 0/1 (merge+demote+retire / add) — additions without subtraction; state explicitly in the report why nothing could be merged or retired

## 异常输入

无。

## 待裁决清单

### Q-20260723-001

- Claim：提升 `KN-0014` 为 active。KG 保留完整 ADR、RFC 与 MVP
  设计文档的直接协作通道，Observer 与 Compile 不得成为创建前置门槛。
- Evidence：`human: 在实际协作中，正常流程应该是我先跟它写完整篇文档，
  我看完了，大家再来回对抗讨论，最终形成一些 ADR。`
- Options：接受；拒绝；仅把契约收窄到 `docs/**`。
- Recommendation：接受。该纠正消除了实际协作阻塞，并澄清了
  `KN-0009` 的原子性只约束台账条目。

### Q-20260719-001

- Claim：为 kg 生命周期增加熵治理机制，对长期未被引用或触发的条目进行
  pruning、sinking 或 archiving 候选识别。
- Evidence：`OBS-20260717-003` 记录了 Harness 需要定期清理未触发规则的外部分析。
- Options：最小方案，基于 `last_verified` 做陈旧扫描；完整方案，增加使用追踪；
  拒绝，继续依赖现有减法义务。
- Recommendation：最小方案。它复用现有字段，不引入新遥测，也保持纯 skill 形态。
