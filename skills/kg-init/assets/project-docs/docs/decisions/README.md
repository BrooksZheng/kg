# Architecture decisions

Create one Markdown file per significant decision. Use
`0000-template.md` as the starting point.

Recommended names use a numeric prefix and imperative phrase:

```text
0001-use-postgresql-for-orders.md
0002-separate-command-and-query-models.md
```

Keep rejected and superseded decisions so future contributors can recover the
reasoning history.
